<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>시험 관리 - FBEye</title>
        <link rel="stylesheet" href="/stylesheets/bootstrap.min.css">
    </head>
    <body>
<div class="container">
    <h3>시험 관리</h3>
    <p class="text-right"><a href="/exams/new" class="btn btn-primary">+ 시험 추가</a></p>
    <table class="table table-hover">
        <thead class="thead-light">
            <tr>
                <th scope="col">시험 제목</th>
                <th scope="col">코드</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            <% if (exams.length > 0) { %>
                <% exams.forEach(e => { %>
                    <% if (e) { %>
                        <tr class="justify-content-center">
                            <td class="align-middle"><a href="/exams/exam/<%= e.accessCode %>"><%= e.title %></a></td>
                            <td class="align-middle"><%= e.accessCode %></td>
                            <td class="text-right"><a href="/exams/supervise/<%= e.accessCode %>" class="btn btn-outline-primary">시험 시작</a></td>
                        </tr>
                    <% } %>
                <% }); %>
            <% } else { %>
                <tr>
                    <td>시험이 없습니다.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
    <p class="text-right"><a href="/" class="btn btn-light">확인</a></p>
</div>
    </body>
</html>
