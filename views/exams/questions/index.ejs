<!DOCTYPE html>
<html>
    <head>
        <title>문제 관리 - FBEye</title>
        <% include ../../common/head %>
    </head>
    <body>
<% include ../../common/sidebar %>
<div class="container">
    <div class="mt-3"></div>
    <h3>문제 관리</h3>
    <p class="text-right"><a href="/exams/questions/new/<%= id %>" class="btn btn-primary">+ 문제 추가</a></p>
    <table class="table table-hover">
        <thead class="thead-light">
            <tr>
                <th scope="col">번호</th>
                <th scope="col">문제</th>
                <th scope="col">형식</th>
                <th scope="col">배점</th>
            </tr>
        </thead>
        <tbody>
            <% if (questions.length > 0) { %>
                <% questions.forEach(e => { %>
                    <% if (e) { %>
                        <tr>
                            <td><%= questions.indexOf(e) + 1 %></td>
                            <td><%= e.question %></td>
                            <td><%= e.type %></td>
                            <td><%= e.score %></td>
                        </tr></a>
                    <% } %>
                <% }); %>
            <% } else { %>
                <tr>
                    <td>문제가 없습니다.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
    <p class="text-right"><a href="/exams/exam/<%= id %>" class="btn btn-light">✔ 확인</a></p>
</div>
    </body>
</html>
